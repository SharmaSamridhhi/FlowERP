# Templates

Page-level layout skeletons (e.g., the app shell with sidebar + header + content slot, the centered auth layout) composed from organisms/molecules/atoms, with content passed in rather than hardcoded. Consumed by `src/pages`.
