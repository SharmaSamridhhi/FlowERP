POSTGRES_USER=flowerp
POSTGRES_PASSWORD=flowerp_local_dev_password
POSTGRES_DB=flowerp
POSTGRES_PORT=5432
BACKEND_PORT=4000
FRONTEND_PORT=8080
JWT_SECRET=replace-with-a-long-random-local-dev-secret
JWT_EXPIRES_IN=8h
SEED_USER_PASSWORD=FlowERP123!

# Product image upload (FLO-024) — optional. Leave unset for a fully
# functional stack minus the image-upload endpoints.
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
